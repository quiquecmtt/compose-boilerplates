# Non-expiring OAuth client secret (from admin console: Settings → OAuth clients, scoped to devices:write, tagged tag:container)
TS_AUTHKEY=tskey-client-REPLACE_ME:REPLACE_SECRET
TS_EXTRA_ARGS=--advertise-tags=tag:container
TRAEFIK_IP= # Normally I use the tailscale IP of the host
TS_STATE_DIR=/var/lib/tailscale
TS_USERSPACE=false
TS_TLD=